# Inbuilt functions on strings:
var="python Programming"

# Inbuilt function with value return type
# print(var.upper()) It is used to convert all the characters in a string to uppercase.
# print(var.lower()) It is used to convert all the characters in a string to lowercase.
# print(var.swapcase()) It is used to swap the case of all the characters in a string.
# print(var.capitalize()) It is used to capitalize the first character of a string and convert all other characters to lowercase.
# print(var.title()) It is used to capitalize the first character of each word in a string

# Inbuilt function with boolean return type
# var="python"
# print(var.isupper()) It is used to check if all the characters in a string are uppercase. It returns True if all characters are uppercase, otherwise it returns False.
# print(var.islower()) It is used to check if all the characters in a string are lowercase. It returns True if all characters are lowercase, otherwise it returns False.
# print(var.istitle()) It is used to check if the first character of each word in a string is uppercase and all other characters are lowercase. It returns True if the condition is met, otherwise it returns False.
# print(var.isdigit()) It is used to check if all the characters in a string are digits. It returns True if all characters are digits, otherwise it returns False.
# print(var.isalpha()) It is used to check if all the characters in a string are alphabetic. It returns True if all characters are alphabetic, otherwise it returns False.
# print(var.isalnum()) It is used to check if all the characters in a string are alphanumeric (letters and numbers). It returns True if all characters are alphanumeric, otherwise it returns False.
# print(var.isspace()) It is used to check if all the characters in a string are whitespace characters. It returns True if all characters are whitespace, otherwise it returns False.
# print(var.startswith("Py")) It is used to check if a string starts with a specified prefix. It returns True if the string starts with the prefix, otherwise it returns False.
# print(var.endswith('on')) It is used to check if a string ends with a specified suffix. It returns True if the string ends with the suffix, otherwise it returns False.

# var="I am ironman"

# print(var.find("dup")) It is used to find the index of the first occurrence of a specified substring in a string.
#  It returns the index if found, otherwise it returns -1.

# var="python "

# new_var=var.replace("python","java") # It is used to replace a specified substring with another substring in a string. It returns a new string with the replacements made.
# print(new_var)
# print(var.strip()) It is used to remove leading and ending whitespace characters from a string. It returns a new string with the whitespace removed.
# print(var.lstrip()) It is used to remove leading whitespace characters from a string. It returns a new string with the leading whitespace removed.
# print(var.rstrip()) It is used to remove trailing whitespace characters from a string. It returns a new string with the trailing whitespace removed.
# var="python programming"
# print(var.split(" ")) It is used to split a string into a list of substrings based on a specified delimiter. It returns a list of substrings.



